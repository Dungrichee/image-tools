import { createSlice } from '@reduxjs/toolkit';

const restorationSlice = createSlice({
    initialState: {},
    name: 'restoration',
    reducers: {},
});

const { reducer } = restorationSlice;
export default reducer;
